# Daily Log — 2026-09-19

## Authorized G01/G02 guardrail repair

- Task: Fix B0's two confirmed guardrail defects, using an isolated E: checkout and preserving live C: Coop.
- Implementation: Fail closed on escaping enforcement/approval exceptions with a fixed reason; replace persisted command details with classifications; suppress legacy command details in the audit display without rewriting history.
- Files: `extensions/coop-guardrails/index.ts`, `tests/guardrails.test.mjs`, README, guardrail/architecture documentation, Unreleased changelog, `docs/COOP_GUARDRAIL_REPAIR.md`, this log.
- Backup: `.backups/guardrail-repair` contains the seven pre-edit files. Receipt/log are new files.
- Standards: AGENTS, project contract, coop-workflow and operational guardrails followed. Auto-documentation applied to affected runtime documentation; no data objects, lineage or generated analytics site require regeneration. Daily-logger storage uses the configured project-log path on E: under the user's explicit E: preference; no C: shared-log write or commit.
- Validation: Reproduced both defects with red tests before their respective fixes; 90 registered-handler test groups pass after repair. Bash syntax, parity and BOM pass. Full suite stops on an unchanged standards fixture's Windows symlink EPERM; no tests disabled or machine permissions changed. Diff/reference checks completed.
- Objects/services affected: Local code/tests/docs only; represented tool executors remain inert in regression tests. No client data, credentials, live queries, installations or upgrades.
- Next action: Review patch; obtain full-suite Windows evidence before merge/release acceptance. B1, MCP version reconciliation, TeamAI, Jev and Desktop remain separate.
- Commit: None. Source HEAD remains `a339dff88a66a53b884e26bd903faad272f2cb21`.

## Summary

- Completed: Both authorized local repairs and regression coverage.
- Unresolved validation: Full-suite Windows symlink permission requirement.
- Stable installation changes: Zero.


## Authorized G03 extension to the existing repair

- Preserved G01/G02 and backed up the repair before adding dynamic MCP wrapper normalization.
- Normalized actual dispatched arguments and server/tool identity for mutation, SQL, scope and grant checks. Bound matching calls reuse one approval; expansions, rejection, revocation and sessions are explicitly tested.
- Added `tests/guardrails-pi-runner.test.mjs` through real Pi 0.84.3 AgentSession/ExtensionRunner hooks; wired it into paired matrix scripts, preserving the PowerShell BOM. Only synthetic offline tools and E: test output were used.
- Updated operational docs, Unreleased changelog and the combined repair receipt. No client identifiers or payloads were copied from the supplied rehearsal.
- Validation: G03 reproduced against the prior bundle; 94 test groups and real Pi hook integration pass. Static/parity/BOM and PowerShell parse checks pass. The full suite still stops at unchanged standards-rev9.test.mjs:250 on a Windows symlink EPERM. No installs, upgrades, permission changes or test bypasses.
- Monday presentation: conditional PR/merge authorization noted, but merge readiness is not met. Prepared a local PR description; no commit, push, PR or merge. No live C: source/profile/package/launcher changes. B1, TeamAI/Jev, Desktop and deployment stay out of scope.


## Symlink validation follow-up — preserve Monday baseline

- User authorized resolving the Windows symlink blocker without disrupting the live installation.
- Used an elevated, hidden test helper confined to E: fixtures/logs. Genuine file-symlink smoke test and the original SECURITY fixture now pass; all 22 standards Revision 9 tests pass. No Developer Mode or permanent privilege changes.
- Corrected only the local work harness: increased its outer timeout from three to twenty minutes, streamed output, selected the existing Python 3.12.14 runtime read-only, and preserved the Windows OS marker. No application code or test assertions changed; no packages installed/upgraded. Python bytecode writes remain disabled.
- Broader suite passes 94 guardrail groups plus standards, fleet/Python, MCP config and request-header checks, then stops at fabric-mcp-launch.test.sh initial-token (exit 41). A focused OS-marker follow-up reproduces 41. The expected token is synthetic; no real sign-in, credentials or remote client requests were used to work around it.
- Updated repair receipt and local PR description; added E: validation evidence. Full-suite/CI acceptance remains incomplete, so no commit, PR, merge, release or deployment.
- Final read-only check: operational C: checkout is clean at 9e8248a8b34a0bd7581b1909bf4fd18f253b3350. Monday's installed source/profile/launcher/package baseline was not changed.

## PR preparation follow-up — current disposition

- The user explicitly requested resolving the blockers and preparing a PR, with later merge acceptable to protect Monday. This supersedes the earlier publication hold; merge/deployment remain held pending acceptance.
- Resolved the synthetic-token fixture using a native E: Python 3.12 launcher from `venv --without-pip`. The earlier Bash wrapper could not be spawned directly by Node on Windows. No packages or production auth code changed; focused fixture passes.
- Full elevated suite now reaches knowledge sync and stops because the workstation has no C compiler for its required Windows fixture. No test was weakened or skipped. Hosted CI must supply the remaining native/compiler, full-suite and ShellCheck evidence.
- Symlink smoke/standards, 94 guardrail test groups, real Pi 0.84.3 hooks, Bash syntax/parity/BOM and PowerShell 5.1/7 parsing passed. No live provider session or remote mutation was used.
- Prepared the repair for a draft PR, separate from B0/optional-package roadmap documentation. The user requires GPT models through OpenAI subscriptions for work. No dependency/version changes, TeamAI/Jev activation, B1 or Desktop implementation.
- Keep the operational C: source/profile intact for Monday; a later deployment needs isolated real-session and rollback acceptance.
