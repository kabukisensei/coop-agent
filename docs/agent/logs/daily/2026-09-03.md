# Daily Log — 2026-09-03

## Summary

Updated the default project contract so each deployment environment can identify
separate Warehouse/Lakehouse and semantic-model workspaces.
Deferred optional data-doc setup so it never interrupts Coop startup or project setup.
Made normal Coop startup go directly to the prompt while retaining every manual
setup path and discovery, partial, mixed, and connected estate option.

## Tasks completed

| Time | Repo | Object / file | Summary | Docs updated |
|------|------|---------------|---------|--------------|
| 08:49 CDT | coop-agent | Project contract workspace layout | Added separate DEV/TEST/PROD environment mappings under `fabric` and `power_bi` across templates and both setup paths | Yes |
| 15:51 CDT | coop-agent | Data-doc setup entry points | Removed automatic session/project setup prompts; retained `/setup-docs`, `/start` → *Document my data*, and `coop data-doc setup` | Yes |
| 16:26 CDT | coop-agent | Direct startup and on-demand setup | Removed automatic Start Here and missing-project dialogs; retained `/start`, `/setup-project`, `/setup-docs`, and all estate shapes | Yes |

## Source changes completed

| File | Summary | Status |
|------|---------|--------|
| `.coop/project.yml`, `.coop/project.example.yml` | Separate Warehouse/Lakehouse and semantic-model workspace mappings | Committed and pushed |
| `lib/init_wizard.py`, `extensions/coop-tools/index.ts` | Generate the two-workspace-per-environment layout from terminal and in-Coop setup | Committed and pushed |
| Project wizard, init wizard, and workflow tests | Prevent the two generated environment maps from drifting | Committed and pushed |
| `README.md`, `CHANGELOG.md` | Document the layout, safe DEV defaults, and non-overwriting update behavior | Committed and pushed |
| `extensions/coop-tools/index.ts`, `tests/project-wizard.test.mjs` | Keep data-doc setup out of startup while preserving manual commands | Committed and pushed |
| Data-doc onboarding, architecture, extension, and tool-contract docs | Describe setup as optional and explicitly user-invoked | Committed and pushed |
| Coop tools startup hook and Start Here tests | Start directly at the prompt; keep setup commands and estate modes available on demand | Completed |
| User, onboarding, architecture, UI, extension, and web docs | Describe direct startup and on-demand setup consistently | Completed |

## Standards / quality findings

- Shell syntax and ShellCheck checks passed.
- `bash scripts/check-parity.sh` passed.
- `bash tests/run.sh` and `pwsh -NoProfile -File tests/run.ps1` passed.
- `git diff --check` passed.
- Focused direct-startup, project-wizard estate-mode, Start Here, model-login,
  and `/setup-docs` bridge tests passed.

## Open questions

- None.

## Next suggested actions

- Monitor the remote checks after the direct-startup fix is pushed.
