# 2026-09-14

## Warehouse MCP and Microsoft Skills Catalog

- Added distinct `fabric-sqlendpoint` managed remote HTTP MCP generation, with global/item endpoint selection from canonical project IDs and no bearer-token config.
- Added approval-first guardrail coverage for direct/proxied Warehouse SQL MCP calls, classifying DDL/DML/destructive SQL before row-read handling and avoiding raw SQL/args in audit logs.
- Replaced the manual Microsoft skills fetch path with a stdlib catalog helper using pinned manifest metadata, immutable generations, LKG launch resolution, and baseline/restricted/disabled policy tests.
- Updated onboarding/init/project templates, doctor/sync reporting, and docs for Warehouse SQL MCP, native OAuth, catalog pins, and live auth/target caveats.
- Verification: `ruff check`, `ruff format --check`, Python compile, Bash syntax, resource validation, parity/BOM, focused suites, full `bash tests/run.sh`, and `pwsh -NoProfile -File tests/run.ps1` all passed.

## P0 Repair Audit Follow-Up

- Hardened the Microsoft skills catalog so refresh failures return non-zero, existing generations are immutable/verified instead of deleted, receipts hash full skill trees, manifest authority is explicit, and launch reads only verified catalog paths.
- Expanded local deterministic coverage for exact-pinned Git fixture refresh, LKG/offline/lock/tamper handling, supply-chain validation, path/frontmatter/symlink rejection, selective launch dirs, and PowerShell suite invocation.
- Tightened Warehouse MCP doctor helpers for managed remote HTTP shape validation, token-free argv, bounded MCP initialize/tools-list probing, registered target vs project target mismatch, auth-required/item-target states, and P0 vertical-slice receipt coverage.
- Verification: focused Python/MCP tests, Python compile, Bash syntax, parity/BOM, full `bash tests/run.sh`, and full `pwsh -NoProfile -File tests/run.ps1` passed locally; no commits, pushes, CI, live Fabric calls, login, or `/root/coop-ops` changes were performed.

## P0 Remediation Completion

- Replaced the remaining config-only Warehouse doctor checks in Bash/PowerShell with the shared `lib/warehouse_mcp.py` doctor path, added bounded MCP protocol coverage including silent-child timeout, and made registered/project URL mismatches report `target_invalid`.
- Reapplied current project policy during Microsoft skills LKG launch resolution, removed stale repo-local loader behavior, enriched catalog doctor output, and updated docs/templates away from manual fetch/cache/copy wording toward `coop sync` + pinned catalog.
- Added deterministic Warehouse MCP, Microsoft catalog policy, and P0 vertical-slice acceptance fixtures to both Bash and PowerShell runners; removed generated `__pycache__` directories after verification.
- Verification: focused fixtures, Python compile, Bash syntax, ShellCheck, resource validation, parity/BOM, `git diff --check`, full `pwsh -NoProfile -File tests/run.ps1`, and full escalated `bash tests/run.sh` all passed. No commits, pushes, CI, live Fabric calls, login, or `/root/coop-ops` changes were performed.

## Windows Knowledge-Git Test Portability

- Made mocked Linux/macOS process-inspector tests independent of host UID APIs and dispatch, while retaining exact present/uncertain/absent and same-user assertions.
- Kept payload secret-stripping as a real cross-platform subprocess test while explicitly preserving Windows job-close fail-closed behavior, and split descriptor rejection from the genuinely POSIX symlink-chain integration.
- Verification: the focused suite (56 tests), a nine-case Windows-path/missing-`geteuid` simulation, full Bash and PowerShell suites, scoped Ruff format/check, Python compile, ShellCheck, parity/BOM, Bash syntax, and `git diff --check` passed. Repo-wide Ruff still reports unrelated pre-existing formatting/lint debt.
