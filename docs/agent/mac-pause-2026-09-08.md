# Coop Desktop pause checkpoint — September 8, 2026

The user requested a stable checkpoint and pause to conserve weekly usage. Stop
implementation and unattended follow-up until the user explicitly resumes.
The overall goal remains incomplete; this is a development checkpoint.

## Repositories and commits

- Mac: `/Users/aaronjennings/Developer/coop-agent-desktop`, branch
  `feature/coop-desktop-platform`. Last pushed commit is
  `c95db47b7237ca2b5228a5325f824c37e6538cb4`. The local checkpoint containing this
  note preserves the additional ownership changes. It is intentionally not pushed
  at pause, so no new CI run is launched. Use `git log -1` for its exact commit.
- VM: verified remote checkpoint `262a9dc068caa1b13e8b958151ccbfded6b97320`, branch
  `feature/coop-desktop-windows-validation-worktrees`. Read its
  `docs/agent/windows-pause-2026-09-08.md` before resuming. Its full suites were
  intentionally interrupted for the pause; do not treat it as fully verified.
- PR 48 remains open and draft. No release, main merge, version bump or tag.

## Latest evidence

- c95db47 managed run 34203950061 finished: Mac passed. Windows isolated runtime
  startup, packaged renderer/chat readiness, and installer construction passed.
  Install/repair/uninstall acceptance failed with an owned command timeout
  after approximately 180 seconds. Inspect the receipt before deciding which
  installer phase failed; do not infer from the aggregate step name.
- c95db47 regular run 34203949925 finished with Windows test failures because
  `realpathSync` preserved the 8.3 TEMP spelling while PowerShell expanded it.
  The helper/bootstrap completed. Next repair the directory-identity assertion
  (for example OS file identity), preserving a meaningful location check.
- Current local ownership patch preserves approved concurrent writers after
  primary release, blocks stale recovery until all owners are gone, and supports
  immediate restart after final release. It also rejects mismatched workspace or
  primary-lease references. Focused workspace tests pass 12 checks; separate real
  processes confirmed retention, third-writer refusal, immediate restart and exit.
- Full PowerShell and lint/syntax/parity checks pass. Full Bash stopped at the
  same-folder Web bridge assertion described in the daily log; its expectation
  has been aligned with the preserved lease behavior, and the focused Web bridge
  recheck exited 0. All local test handles are terminal. A full suite on the final
  checkpoint remains required when resuming.

## Preserved work and next actions

1. Read this note, the VM pause note and the daily log; inspect actual Git state.
2. Reconcile the VM's newer worktree, updater and interaction changes selectively.
   Do not merge the broad older validation branch blindly over the current fixes.
3. Correct the Windows directory-identity test and run fresh full Bash/PowerShell
   checks on the combined tree. Use explicit Homebrew Node/npm on this Mac.
4. Investigate the installer timeout, rebuild isolated packages, and repeat the
   remaining installed journeys and update/recovery/rollback acceptance.
5. Continue the full completion ledger; startup success alone is not completion.

Evidence: `/private/tmp/coop-desktop-home-20260907-state/`, especially
`workspace-writer-release-*`, `windows-c95db47-*` and the previous startup logs.
Ownership backups: `.backups/workspace_writer_release_20260908_032113/`.

Keep authenticated Mac and VM validation profiles in place. Do not copy/read
credentials. No new timers, automations, agents or background builds are scheduled.
No local test process should remain after the final focused check is collected.
